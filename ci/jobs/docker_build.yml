docker_build:
  stage: docker_build
  image: docker:latest 
  services:
    - docker:dind
  script:
    - ci/scripts/build_n_push.sh 
  artifacts:
    paths:
      - $LOGFILE
  only:
    - main
  needs: ["docker_linting", "semgrep_scan"]
