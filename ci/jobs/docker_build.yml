docker_build:
  stage: docker_build
  image: docker:latest 
  services:
    - docker:dind
  script:
    - ../scripts/build_docker_image.sh

  only:
    - main
  needs: ["docker_linting", "semgrep_scan"]
